name: Build Yggstack AAR

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # Permite rodar manualmente

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout local repo
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26' # Atualizado para a versão que o mobile exige

      - name: Setup Java
        uses: actions/setup-java@v5 # Atualizado para v5 (reparei que a v4 deu um aviso de descontinuada)
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android NDK
        run: sdkmanager "ndk;25.2.9519653"

      - name: Download Go dependencies
        run: |
          # Arranca a versão corrompida do arquivo ANTES do Go tentar ler
          go mod edit -droprequire golang.org/x/mobile
          
          # Busca a versão mais recente e correta
          go get golang.org/x/mobile@latest
          
          # Declara explicitamente a ferramenta de bind conforme exigido no Go 1.26+
          go get -tool golang.org/x/mobile/cmd/gobind
          
          go mod tidy
          
          # Baixa as dependências do módulo Go
          go mod download

      - name: Install Gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest
          
          # Garante que as ferramentas do Go estejam no PATH
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH
          export PATH=$PATH:$(go env GOPATH)/bin
          
          # Inicializa o Gomobile
          gomobile init

      - name: Build AAR
        run: |
          export ANDROID_NDK_HOME=$ANDROID_HOME/ndk/25.2.9519653
          
          # O comando 'gomobile bind' cria o .aar
          # Ele precisa apontar para o pacote Go que contém as funções exportáveis (ex: cmd/yggstack)
          gomobile bind -target=android -o yggstack.aar ./cmd/yggstack

      - name: Upload AAR Artifact
        uses: actions/upload-artifact@v4
        with:
          name: yggstack-android
          path: yggstack.aar
